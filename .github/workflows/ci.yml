name: CI/CD Matrix Pipeline

on:
  push:
    tags: ['v*']

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v3
    - name: Run Build
      run: |
        # Здесь была бы логика для разных систем
        echo "Building on ${{ matrix.os }}"
